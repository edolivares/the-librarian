
<%= simple_form_for(@book, html: { class: '' }) do |f| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="form-group">
      <%= f.input :title %>
   </div>

   <div class="form-group">
      <%= f.input :author %>
   </div>

   <div class="form-group">
      <%= f.input :editorial %>
   </div>

   <div class="form-group">
      <%= f.input :original_title %>
   </div>

   <div class="form-group">
      <%= f.input :translation %>
   </div>

   <div class="form-group">
      <%= f.input :edition %>
   </div>

   <div class="form-group">
      <%= f.input :edition_date %>
   </div>

   <div class="form-group">
      <%= f.input :edition_place %>
   </div>

   <div class="form-group">
      <%= f.input :publication_year %>
   </div>

   <div class="form-group">
      <%= f.input :isbn %>
   </div>

   <div class="form-group">
      <%= f.button :submit, :class => 'btn-success btn-lg btn-block'  %>
   </div>

<% end %>
